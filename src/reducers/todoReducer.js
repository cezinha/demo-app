const initialState = {
  'list': []
}
const todoReducer = (state = initialState, action) => {
  switch(action.type) { 
    case 'ADD_TODO':
    default:
      return state;
  }
}
export default todoReducer;